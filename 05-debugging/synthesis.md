In the browser, the dev tools allow debugging. Even if the browser isn't running the actual code writen, it's still possible to debugg and execute the code source.

Besides, there is the "Angular DevTools" provided by the angular team to manage better debugging with component hierarchy, or data I/O.